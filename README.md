# This project was made with Create React App

## Setup

To setup and run this project, do the following:

1. Run `npm install` (ensure that node and npm are both installed on your machine)
2. Run `npm start` to spin up a dev server

